enum CLOSET {
  OPEN_INVENTORY,
}

enum CALENDAR {
  SHOW_DAY,
  NEXT_MEETING,
}

enum EMAIL {
  SHOW_UNREAD,
  COUNT_UNREAD,
}

enum WEATHER {
  CURRENT,
}

enum REMINDER {
  ADD,
  CHECK,
}

enum DIRECTIONS {
  OPEN_DETAILED_MAP,
  GET_DISTANCE,
  GET_TRAVEL_TIME,
}

enum SCHEDULE {
  WHAT_AT_GIVEN_TIME,
}

type EVENTS = {
  CLOSET: CLOSET;
  CALENDAR: CALENDAR;
  EMAIL: EMAIL;
  WEATHER: WEATHER;
  REMINDER: REMINDER;
  DIRECTIONS: DIRECTIONS;
  SCHEDULE: SCHEDULE;
};

export default EVENTS;
